# Google Calendar FastAPI 서버 환경 설정 파일
# 이 파일을 .env로 복사한 후 실제 값을 입력하세요

# 서버 설정
PORT=9000
HOST=0.0.0.0

# 환경 설정
ENVIRONMENT=development
DEBUG=True

# 로그 설정 (선택사항)
LOG_LEVEL=INFO
LOG_FILE=calendar_server.log

# Google Calendar API 설정 (자동 생성되는 파일들)
# CREDENTIALS_FILE=credentials.json  # Google Cloud Console에서 다운로드
# TOKEN_FILE=token.pickle            # OAuth 인증 후 자동 생성

# 보안 설정 (프로덕션 환경에서만)
# SECRET_KEY=your-very-secure-secret-key-here
# CORS_ORIGINS=http://localhost:3000,https://yourdomain.com

# 캘린더 설정
# DEFAULT_TIMEZONE=Asia/Seoul
# WORKING_HOURS_START=09:00
# WORKING_HOURS_END=18:00

# 알림 설정 (향후 확장용)
# EMAIL_NOTIFICATIONS=False
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...

# 데이터베이스 (향후 확장용)
# DATABASE_URL=sqlite:///./calendar_bot.db